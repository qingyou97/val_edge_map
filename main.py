x_min = min(rotated_points, key=lambda p: p[0])[0]
    x_max = max(rotated_points, key=lambda p: p[0])[0]
    y_min = min(rotated_points, key=lambda p: p[1])[1]
    y_max = max(rotated_points, key=lambda p: p[1])[1]
